{
  "name": "ejercitacion-mongodb-driver",
  "version": "1.0.0",
  "description": "Ejercitación de ExpressJS interactuando con MongoDB mediante el Driver oficial",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --watch --env-file=.env server.js",
    "test": "jest --detectOpenHandles"
  },
  "dependencies": {
    "express": "^4.22.2",
    "mongodb": "^6.21.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "mongodb-memory-server": "^10.0.0",
    "supertest": "^7.0.0"
  }
}
